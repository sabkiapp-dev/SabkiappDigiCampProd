[1000000090]
exten => s,1,Verbose(1,Call is answered – campaign ${DIALPLAN})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${NAME} &)
 same => n,Playback(initial_wait)
 same => n,Goto(1000000090-1,s,1)

; ---------------------------------------------------------------------
; 1000000090-1 – static intro + three dynamic clips
; ---------------------------------------------------------------------
[1000000090-1]
exten => s,1,Verbose(1,Started Extension 1 of campaign 1000000090)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=1)
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/01_Namskar)
 same => n,Playback(/var/lib/asterisk/sounds/hi/dynamic/${NAME})
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/02_yeh_call)
 same => n,Playback(/var/lib/asterisk/sounds/hi/dynamic/${CHANNEL_NAME})
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/03_Channel)
 same => n,Playback(/var/lib/asterisk/sounds/hi/dynamic/${SURVEYOR_NAME})
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/04_Dwara)
 same => n,Goto(1000000090-2,s,1)

; ---------------------------------------------------------------------
; 1000000090-2 – option menu (channel hash spoken 3×)
; ---------------------------------------------------------------------
[1000000090-2]
exten => s,1,Verbose(1,Started Extension 2 of campaign 1000000090)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=2)

 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/05_Agar_aap)
 same => n,BackGround(/var/lib/asterisk/sounds/hi/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/06_Option_Voice)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/05_Agar_aap)
 same => n,BackGround(/var/lib/asterisk/sounds/hi/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/06_Option_Voice)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/05_Agar_aap)
 same => n,BackGround(/var/lib/asterisk/sounds/hi/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/hi/static/06_Option_Voice)
 same => n,BackGround(wait)

 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

; ---------------------------------------------------------------------
; DTMF: 1 = success, 2 = cancel, others repeat menu
; ---------------------------------------------------------------------
exten => 1,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/07_Success)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => 2,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/hi/static/08_Cancel)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => _[03-9],1,Playback(/var/lib/asterisk/sounds/hi/static/09_wrong_input)
 same => n,Goto(1000000090-2,s,1)

exten => 0,1,Playback(/var/lib/asterisk/sounds/hi/static/09_wrong_input)
 same => n,Goto(1000000090-2,s,1)




;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;



[1000000092]
exten => s,1,Verbose(1,Call is answered – campaign ${DIALPLAN})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${NAME} &)
 same => n,Playback(initial_wait)
 same => n,Goto(1000000090-1,s,1)

; ---------------------------------------------------------------------
; 1000000090-1 – static intro + three dynamic clips
; ---------------------------------------------------------------------
[1000000092-1]
exten => s,1,Verbose(1,Started Extension 1 of campaign 1000000092)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=1)

 ; verbose some text 'hiiiiii'
 same => n,Verbose(1,hiiiiii)

 same => n,Playback(/var/lib/asterisk/sounds/en/static/En_01_Hello)
 same => n,Playback(/var/lib/asterisk/sounds/en/dynamic/${NAME})
 same => n,Playback(/var/lib/asterisk/sounds/en/static/En_02_Call)
 same => n,Playback(/var/lib/asterisk/sounds/en/dynamic/${CHANNEL_NAME})
 same => n,Playback(/var/lib/asterisk/sounds/en/static/En_03_Channel)
 same => n,Playback(/var/lib/asterisk/sounds/en/dynamic/${SURVEYOR_NAME})
 same => n,Goto(1000000092-2,s,1)

; ---------------------------------------------------------------------
; 1000000090-2 – option menu (channel hash spoken 3×)
; ---------------------------------------------------------------------
[1000000092-2]
exten => s,1,Verbose(1,Started Extension 2 of campaign 1000000092)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=2)

 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_04_Join)
 same => n,BackGround(/var/lib/asterisk/sounds/en/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_05_Option_Voice)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_04_Join)
 same => n,BackGround(/var/lib/asterisk/sounds/en/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_05_Option_Voice)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_04_Join)
 same => n,BackGround(/var/lib/asterisk/sounds/en/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/en/static/En_05_Option_Voice)
 same => n,BackGround(wait)

 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

; ---------------------------------------------------------------------
; DTMF: 1 = success, 2 = cancel, others repeat menu
; ---------------------------------------------------------------------
exten => 1,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/en/static/En_06_Success)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => 2,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/en/static/En_07_Cancel)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => _[03-9],1,Playback(09_wrong_input)
 same => n,Goto(1000000092-2,s,1)

exten => 0,1,Playback(09_wrong_input)
 same => n,Goto(1000000092-2,s,1)


;======================================================================
; 1000000093 – Oriya verification campaign
;======================================================================

[1000000093]
exten => s,1,Verbose(1,Call is answered – campaign ${DIALPLAN})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${NAME} &)

 ; ── greeting pause (keep the old one or create an Oriya version) ──
 same => n,Playback(initial_wait)

 same => n,Goto(1000000093-1,s,1)

; -------------------------------------------------------------------
; 1000000093-1 – static intro + three dynamic clips
; -------------------------------------------------------------------
[1000000093-1]
exten => s,1,Verbose(1,Started Extension 1 of campaign 1000000093)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=1)

 ;── static + dynamic sequence ───────────────────────────────────────
 same => n,Playback(/var/lib/asterisk/sounds/or/static/01_Namste)          ;🖉
 same => n,Playback(/var/lib/asterisk/sounds/or/dynamic/${NAME})
 same => n,Playback(/var/lib/asterisk/sounds/or/static/03_yeh_call)        ;🖉
 same => n,Playback(/var/lib/asterisk/sounds/or/dynamic/${CHANNEL_NAME})
 same => n,Playback(/var/lib/asterisk/sounds/or/static/05_Channel)         ;🖉
 same => n,Playback(/var/lib/asterisk/sounds/or/dynamic/${SURVEYOR_NAME})
 same => n,Playback(/var/lib/asterisk/sounds/or/static/07_Dwara)           ;🖉
 same => n,Goto(1000000093-2,s,1)

; -------------------------------------------------------------------
; 1000000093-2 – option menu (channel hash spoken 3×)
; -------------------------------------------------------------------
[1000000093-2]
exten => s,1,Verbose(1,Started Extension 2 of campaign 1000000093)
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(NAME=${CALLERID(num):32:15})        ; 32-46
 same => n,Set(CHANNEL_NAME=${CALLERID(num):47:15})  ; 47-61
 same => n,Set(SURVEYOR_NAME=${CALLERID(num):62:15}) ; 62-76
 same => n,Set(current_extension=2)

 ;── play the option menu three times ────────────────────────────────
 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)   ;🖉
 same => n,BackGround(/var/lib/asterisk/sounds/or/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)   ;🖉 (or a separate “press 1/2” clip)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)
 same => n,BackGround(/var/lib/asterisk/sounds/or/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)
 same => n,BackGround(wait)

 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)
 same => n,BackGround(/var/lib/asterisk/sounds/or/dynamic/${CHANNEL_NAME})
 same => n,BackGround(/var/lib/asterisk/sounds/or/static/09_option_call)
 same => n,BackGround(wait)

 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

; -------------------------------------------------------------------
; DTMF handlers
; -------------------------------------------------------------------
exten => 1,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/or/static/Success)            ;🖉
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => 2,1,Verbose(1,Current Extension is ${current_extension})
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh dtmf ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} ${current_extension} ${EXTEN} &)
 same => n,Playback(/var/lib/asterisk/sounds/or/static/Cancel)             ;🖉
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

exten => _[03-9],1,Playback(/var/lib/asterisk/sounds/or/static/wrong_input) ;🖉
 same => n,Goto(1000000093-2,s,1)

exten => 0,1,Playback(/var/lib/asterisk/sounds/or/static/wrong_input)
 same => n,Goto(1000000093-2,s,1)


;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
[1000000078]
exten => s,1,Verbose(1, "Call is answered for campaign ${DIALPLAN}")
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Playback(initial_wait)
 same => n,GoTo(1000000078-1,s,1)

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
[1000000078-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign ${DIALPLAN}")
 same => n,Set(current_extension=1)
 same => n,Playback(133)
 same => n,Verbose(1, "Option Voice not available")
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()

[1000000056]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000056-1,s,1)
            
[1000000056-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000056")
 same => n, set(current_extension=1)
 same => n, Playback(117)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000086]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000086-1,s,1)
            
[1000000086-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000086")
 same => n, set(current_extension=1)
 same => n, Playback(162)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000066]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000066-1,s,1)
            
[1000000066-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000066")
 same => n, set(current_extension=1)
 same => n, Playback(124)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000103]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000103-1,s,1)
            
[1000000103-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000103")
 same => n, set(current_extension=1)
 same => n, Playback(167)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000105]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000105-1,s,1)
            
[1000000105-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000105")
 same => n, set(current_extension=1)
 same => n, Playback(168)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000107]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000107-1,s,1)
            
[1000000107-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000107")
 same => n, set(current_extension=1)
 same => n, Playback(169)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000108]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000108-1,s,1)
            
[1000000108-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000108")
 same => n, set(current_extension=1)
 same => n, Playback(151)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000109]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000109-1,s,1)
            
[1000000109-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000109")
 same => n, set(current_extension=1)
 same => n, Playback(170)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000110]
; The Dial 'U' flag passes arguments: ARG2=Phone, ARG3=Port/CallerID, ARG4=UserID, ARG5=Dialplan
exten => s,1,Verbose(1, "Call is answered for campaign ${ARG5}")
 same => n,Set(PHONE=${ARG2})
 same => n,Set(CALLERID_NUM=${ARG3})
 same => n,Set(USER_ID=${ARG4})
 same => n,Set(DIALPLAN=${ARG5})
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Playback(initial_wait)
 same => n,GoTo(1000000110-1,s,1)
            
[1000000110-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign ${DIALPLAN}")
 same => n, Verbose(1, "Current channel variables: PHONE=${PHONE}, CALLERID_NUM=${CALLERID_NUM}, USER_ID=${USER_ID}, DIALPLAN=${DIALPLAN}")
 ; We do not need to extract the variables again here. 
 ; Because we set them as channel variables in the block above, they persist on this channel.
 same => n,Set(current_extension=1)
 same => n,Playback(150)
 same => n,Verbose(1, "Option Voice not available")
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Hangup()
[1000000111]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000111-1,s,1)
            
[1000000111-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000111")
 same => n, set(current_extension=1)
 same => n, Playback(171)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000112]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000112-1,s,1)
            
[1000000112-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000112")
 same => n, set(current_extension=1)
 same => n, Playback(150)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000114]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000114-1,s,1)
            
[1000000114-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000114")
 same => n, set(current_extension=1)
 same => n, Playback(171)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000113]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000113-1,s,1)
            
[1000000113-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000113")
 same => n, set(current_extension=1)
 same => n, Playback(172)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000115]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000115-1,s,1)
            
[1000000115-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000115")
 same => n, set(current_extension=1)
 same => n, Playback(173)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000116]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000116-1,s,1)
            
[1000000116-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000116")
 same => n, set(current_extension=1)
 same => n, Playback(173)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000117]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000117-1,s,1)
            
[1000000117-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000117")
 same => n, set(current_extension=1)
 same => n, Playback(174)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000120]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000120-1,s,1)
            
[1000000120-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000120")
 same => n, set(current_extension=1)
 same => n, Playback(175)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000121]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000121-1,s,1)
            
[1000000121-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000121")
 same => n, set(current_extension=1)
 same => n, Playback(175)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000122]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000122-1,s,1)
            
[1000000122-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000122")
 same => n, set(current_extension=1)
 same => n, Playback(176)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000123]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000123-1,s,1)
            
[1000000123-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000123")
 same => n, set(current_extension=1)
 same => n, Playback(177)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000124]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000124-1,s,1)
            
[1000000124-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000124")
 same => n, set(current_extension=1)
 same => n, Playback(178)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()

[1000000127]
exten => s,1,Verbose(1, "Call is answered" ${campaign})
 same => n,Set(PHONE=${CALLERID(num):0:10})
 same => n,Set(CALLERID_NUM=${CALLERID(num):10:4})
 same => n,Set(USER_ID=${CALLERID(num):14:8})
 same => n,Set(DIALPLAN=${CALLERID(num):22:10})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000127-1,s,1)
            
[1000000127-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000127")
 same => n, set(current_extension=1)
 same => n, Playback(125)
 same => n, Verbose(1, "Option Voice not available")
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh completed ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Hangup()


[1000000126]
exten => s,1,Verbose(1, "Call is answered ${ARG1}")
 ; READ FROM THE ARGUMENTS PASSED BY THE DIAL COMMAND
 same => n,Set(PHONE=${ARG2})
 same => n,Set(CALLERID_NUM=${ARG3})
 same => n,Set(USER_ID=${ARG4})
 same => n,Set(DIALPLAN=${ARG5})
 same => n,Set(TIMEOUT(absolute)=600)
 same => n,System(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n,Playback(initial_wait)
 same => n,GoTo(1000000126-1,s,1)
            
[1000000126-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000126")
 same => n,set(current_extension=1)
 same => n,Playback(180)
 same => n,Verbose(1, "Option Voice not available")
 ; REMOVED the dtmf.sh completed line from here! The 'h' extension in basic-context will handle it.
 same => n,Hangup()
[1000000125]
exten => s,1,Verbose(1, "Call is answered ${ARG1}")
 same => n,Set(PHONE=${ARG2})
 same => n,Set(CALLERID_NUM=${ARG3})
 same => n,Set(USER_ID=${ARG4})
 same => n,Set(DIALPLAN=${ARG5})
 same => n, Set(TIMEOUT(absolute)=600)
 same => n, SYSTEM(/home/pi/Documents/MachineStatus/dtmf.sh answered ${PHONE} ${CALLERID_NUM} ${USER_ID} ${DIALPLAN} &)
 same => n, Playback(initial_wait)
 same => n, GoTo(1000000125-1,s,1)
            
[1000000125-1]
exten => s,1,Verbose(1, "Started Extension 1 of campaign 1000000125")
 same => n, set(current_extension=1)
 same => n, Playback(179)
 same => n, Verbose(1, "Option Voice not available")
 same => n, Hangup()
